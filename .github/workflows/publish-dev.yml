name: Syndicate approved articles to DEV

on:
  workflow_run:
    workflows:
      - Publish GitHub Pages
    types:
      - completed
    branches:
      - main
  workflow_dispatch:
    inputs:
      sync_all:
        description: "Sync every published, DEV-ready article instead of only the latest changed article"
        required: true
        default: true
        type: boolean

permissions:
  contents: read

concurrency:
  group: dev-syndication
  cancel-in-progress: false

jobs:
  syndicate:
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - name: Check out canonical source
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event_name == 'workflow_run' && github.event.workflow_run.head_sha || github.sha }}
          fetch-depth: 2

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Determine automatic sync scope
        id: scope
        if: ${{ github.event_name == 'workflow_run' }}
        shell: bash
        run: |
          if git diff --name-only HEAD^ HEAD -- \
            .github/workflows/publish-dev.yml \
            scripts/syndicate_dev.py | grep -q .; then
            echo "mode=all" >> "$GITHUB_OUTPUT"
          else
            echo "mode=changed" >> "$GITHUB_OUTPUT"
          fi

      - name: Sync all eligible articles after syndication implementation changes
        if: ${{ github.event_name == 'workflow_run' && steps.scope.outputs.mode == 'all' }}
        env:
          DEV_API_KEY: ${{ secrets.DEV_API_KEY }}
        run: python scripts/syndicate_dev.py --all

      - name: Sync article changed by the successful Pages publication
        if: ${{ github.event_name == 'workflow_run' && steps.scope.outputs.mode == 'changed' }}
        env:
          DEV_API_KEY: ${{ secrets.DEV_API_KEY }}
        run: python scripts/syndicate_dev.py --changed

      - name: Sync all eligible articles
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.sync_all }}
        env:
          DEV_API_KEY: ${{ secrets.DEV_API_KEY }}
        run: python scripts/syndicate_dev.py --all

      - name: Sync only the latest changed article
        if: ${{ github.event_name == 'workflow_dispatch' && !inputs.sync_all }}
        env:
          DEV_API_KEY: ${{ secrets.DEV_API_KEY }}
        run: python scripts/syndicate_dev.py --changed
